{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the popup and settings windows",
  "windows": ["popup", "settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "autostart:default"
  ]
}
